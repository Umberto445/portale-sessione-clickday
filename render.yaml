services:
  - type: web
    name: portale-sessione-clickday
    env: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    disk:
      name: portale-sessione-clickday-data
      mountPath: /opt/render/project/src/data
      sizeGB: 1
